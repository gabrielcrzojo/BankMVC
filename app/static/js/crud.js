// JavaScript específico para operações CRUD
document.addEventListener('DOMContentLoaded', () => {
    console.log("CRUD JS carregado.");
    
    // Confirmação antes de submeter forms destrutivos já está inline com onsubmit,
    // mas poderíamos centralizar lógica aqui se o projeto crescer.
});
